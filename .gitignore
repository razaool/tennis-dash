node_modules/
.env
*.log
.DS_Store

# Backups
backups/
tennis_dash_production_backup.sql

# Logs (keep directory, ignore contents)
logs/

# ML model artifacts: trained model + metadata ARE committed (the service loads
# them from the repo). Only the large per-tour feature-extraction caches are ignored.
ml-service/*_features.csv
/model_metadata.json
/scaler.pkl
/xgboost_model.pkl

# Railway/Vercel configs (only needed in specific directories)
/Procfile
/railway.json
/nixpacks.toml
/requirements.txt

# Data files (keep in data-source/)
/*.csv
/*.sql
/*.txt
ml_features.csv

# Temp directories
temp_csv/
temp_sql/
archive/

# Claude Code settings (may contain sensitive data)
.claude/
